# deps / builds
node_modules/
.next/
dist/
*.tsbuildinfo

# env — NEVER commit real keys. The template is .env.docker.example.
.env
.env.*
!.env.docker.example
!.env.example

# harness telemetry (local only)
.harness-logs/

# misc
.DS_Store
npm-debug.log*

# Claude Code workspace — local only, never tracked in this repo
.claude/
